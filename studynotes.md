#应用层
2026-09-12

##一、网络层概述
· 网络层的主要任务是实现网络互联，进而实现数据包在各网络之间的传输
· 要实现网络层任务，需要解决以下主要问题：
   · 网络层向运输层提供怎样的服务（“可靠传输”还是“不可靠传输”）
   · 网络层寻址问题
   · 路由选择问题
· 因特网是目前全世界用户数量最多的互联网，它使用TCP/IP协议栈
· 由于TCP/IP协议栈的网络层使用网际协议IP,它是整个协议栈的核心协议，因此在TCP/IP协议栈中网络层常称为网际层

##二、IPv4地址概述
· IPv4地址就是给因特网上的每一台主机的每一个接口分配一个在全世界范围内是唯一的32比特的标识符
· 32比特的IPv4地址不方便阅读、记录以及输入等，因此IPv4地址采用点分十进制表示方法以方便用户使用

##三、分类编址的IPv4地址
· A类：网络号部分占8比特，主机号部分占24比特，网络号最高位固定为0
· B类：网络号部分和主机号部分各占16比特，网络号的最高两位固定为10
· C类：网络号部分占24比特，主机号部分占8比特，网络号最高三位固定为110
· D类：多播地址，最高四位固定为1110
· E类：保留地址，最高四位固定为1111
只有A、B、C类地址可分配给网络中的主机或路由器的各类接口
主机号为“全0”的地址是网络地址，不能分配给主机或路由器的各接口
主机号为“全1”的地址是广播地址，不能分配给主机或路由器的各接口

0-127为A类
128-191为B类
192-223为C类

以下三种情况的地址不能指派：
· A类网络号0和127
· 主机号为“全0”，这是网络地址
· 主机号为“全1”，这是广播地址






